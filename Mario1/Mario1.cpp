#include "Game.h"

#define WINDOW_WIDTH 1200
#define WINDOW_HEIGHT 900


int main()
{
    //sf::RenderWindow window(sf::VideoMode(WINDOW_WIDTH, WINDOW_HEIGHT), "Load Image Example");
    
    Game game;
    game.run();
    return 0;
}
